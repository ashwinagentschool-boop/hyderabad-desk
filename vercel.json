{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }],
  "headers": [
    {
      "comment": "HTML must never be cached: it is the only file whose name never changes, so a stale copy pins the browser to an old asset hash forever.",
      "source": "/(.*)",
      "headers": [
        { "key": "Cache-Control", "value": "no-store, must-revalidate" }
      ]
    },
    {
      "comment": "Assets are content-hashed by Vite, so a new build always means a new filename. Safe to cache forever. Listed after the catch-all so it wins.",
      "source": "/assets/(.*)",
      "headers": [
        { "key": "Cache-Control", "value": "public, max-age=31536000, immutable" }
      ]
    }
  ]
}
